<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 网络权限 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- NFC权限 -->
    <uses-permission android:name="android.permission.NFC" />
    <uses-feature
        android:name="android.hardware.nfc"
        android:required="false" />

    <!-- 蓝牙权限 -->
    <uses-permission android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="s" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-feature
        android:name="android.hardware.bluetooth_le"
        android:required="false" />

    <!-- UWB权限 -->
    <uses-permission android:name="android.permission.UWB_RANGING" />
    <uses-feature
        android:name="android.hardware.uwb"
        android:required="false" />

    <!-- 位置权限 -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- 相机权限 (扫二维码) -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <!-- 生物识别 -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <!-- 震动 -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:name=".DigitalKeyApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DigitalKey"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="34">

        <!-- 主界面 -->
        <activity
            android:name=".home.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.DigitalKey">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 钥匙详情 -->
        <activity
            android:name=".key.KeyDetailActivity"
            android:parentActivityName=".home.MainActivity"
            android:theme="@style/Theme.DigitalKey" />

        <!-- 添加钥匙 -->
        <activity
            android:name=".key.AddKeyActivity"
            android:parentActivityName=".home.MainActivity"
            android:theme="@style/Theme.DigitalKey" />

        <!-- 分享钥匙 -->
        <activity
            android:name=".key.ShareKeyActivity"
            android:parentActivityName=".key.KeyDetailActivity"
            android:theme="@style/Theme.DigitalKey" />

        <!-- 车控 -->
        <activity
            android:name=".home.VehicleControlActivity"
            android:parentActivityName=".key.KeyDetailActivity"
            android:theme="@style/Theme.DigitalKey" />

        <!-- 设置 -->
        <activity
            android:name=".settings.SettingsActivity"
            android:parentActivityName=".home.MainActivity"
            android:theme="@style/Theme.DigitalKey" />

    </application>

</manifest>
