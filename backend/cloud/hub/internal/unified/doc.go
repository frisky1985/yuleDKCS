// Package unified implements the SWC layer (ASPICE SWE.6)
// Unified protocol handler — normalizes CCC/ICCOA/ICCE protocol flows
// into a single internal pipeline: codec → router → state machine.
package unified
