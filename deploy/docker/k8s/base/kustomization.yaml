apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: yuledkcs

commonLabels:
  app.kubernetes.io/managed-by: kustomize
  app.kubernetes.io/part-of: yuledkcs

resources:
  - namespace.yaml
  - configmap.yaml
  - secret.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - hpa.yaml

images:
  - name: yuledkcs/backend
    newTag: v1.0.0
