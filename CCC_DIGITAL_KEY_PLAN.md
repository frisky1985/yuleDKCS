# yuleDKCS — CCC Digital Key R2.0 示例项目规划

> **定位**: 基于 YuleTech AutoSAR BSW 平台的数字钥匙参考实现
> **关系**: yuleASR = AutoSAR BSW 平台 | yuleDKCS = 平台应用示例
> **协议标准**: CCC Digital Key R3.0 → R4.1
> **硬件平台**: NXP KW47 (BLE/UWB) + SE050 (安全芯片)

---

## 1. 产品定位

### 1.1 核心目标

将 yuleDKCS 打造为 **yuleASR 平台的旗舰示例应用**，示范如何将 AutoSAR BSW 模块（加密、诊断、通信、OS）用于真实的汽车数字钥匙系统。

### 1.2 覆盖范围

```
┌─────────────────────────────────────────────────────────┐
│                    yuleDKCS (示例项目)                     │
│                                                          │
│  ┌──────────────────────────────────┐   ┌─────────────┐  │
│  │      数字钥匙三端系统             │   │  部署运维     │  │
│  │  ┌──────┐ ┌───────┐ ┌────────┐  │   │  Docker      │  │
│  │  │ 嵌入式│ │ 后端  │ │ 移动端  │  │   │  K8s/Helm   │  │
│  │  │ KW47 │ │ Go    │ │ iOS    │  │   │  CI/CD      │  │
│  │  │      │ │ API   │ │ Android│  │   └─────────────┘  │
│  │  │      │ │ MQTT  │ │ Flutter│  │                     │
│  │  └──────┘ └───────┘ └────────┘  │                     │
│  └──────────────────────────────────┘                     │
│                                                          │
│               ┌──────────────────┐                       │
│               │  yuleASR (平台)  │                       │
│               │  BSW/OS/RTE/ASW │                       │
│               └──────────────────┘                       │
└─────────────────────────────────────────────────────────┘
```

### 1.3 与 yuleASR 的清晰分界

| 方向 | yuleASR | yuleDKCS |
|:---|:---|:---|
| **职责** | AutoSAR BSW 平台（通用） | 数字钥匙应用（特定） |
| **代码** | MCAL/ECUAL/Services/RTE/OS/ASW | CCC/ICCE/ICCOA 协议栈 |
| **硬件** | 通用 MCU 抽象 (S32K3/TC3xx) | KW47 专用 (BLE/UWB/NFC) |
| **安全** | CSM/CryIf/KeyM 通用加密接口 | SE050 + mbedTLS 具体实现 |
| **复用** | 平台服务被 yuleDKCS 调用 | 展示如何调用 BSW 服务 |

---

## 2. 系统架构

### 2.1 三端架构总览

```
┌──────────────────────┐     ┌──────────────────────┐     ┌──────────────────────┐
│     嵌入式 (KW47)     │     │    云端后端 (Go)      │     │     移动端 SDK        │
│                      │     │                      │     │                      │
│ ┌──────────────────┐ │ BLE │ ┌──────────────────┐ │ MQTT│ ┌──────────────────┐ │
│ │ CCC Protocol     │ │<────│ │ API Gateway      │ │<────│ │ Digital Key App  │ │
│ │   - 密钥协商      │ │     │ │   - 注册/认证    │ │     │ │   - BLE通信      │ │
│ │   - 身份认证      │ │     │ │   - 车辆管理     │ │     │ │   - 钥匙管理     │ │
│ │   - 安全信道      │ │     │ │   - 钥匙分发     │ │     │ │   - 近场控制     │ │
│ └──────────────────┘ │     │ └──────────────────┘ │     │ └──────────────────┘ │
│ ┌──────────────────┐ │     │ ┌──────────────────┐ │     │                      │
│ │ BLE/UWB Stack    │ │     │ │ MQTT Broker      │ │     │ 平台: iOS + Android   │
│ │   - 广播/扫描    │ │     │ │   - 实时消息     │ │     │                      │
│ │   - 测距服务     │ │     │ │   - 事件推送     │ │     │                      │
│ │   - 连接管理     │ │     │ └──────────────────┘ │     │                      │
│ └──────────────────┘ │     │ ┌──────────────────┐ │     │                      │
│ ┌──────────────────┐ │     │ │ Database (PG)   │ │     │                      │
│ │ Security Stack   │ │     │ │   - 用户        │ │     │                      │
│ │   - SE050        │ │     │ │   - 车辆        │ │     │                      │
│ │   - mbedTLS      │ │     │ │   - 钥匙        │ │     │                      │
│ │   - SCP03        │ │     │ └──────────────────┘ │     │                      │
│ └──────────────────┘ │     │ ┌──────────────────┐ │     │                      │
│ ┌──────────────────┐ │     │ │ Redis Cache      │ │     │                      │
│ │ yuleASR BSW      │ │     │ │   - 会话        │ │     │                      │
│ │   - OS调度       │ │     │ │   - 令牌        │ │     │                      │
│ │   - 加密服务     │ │     │ └──────────────────┘ │     │                      │
│ │   - 诊断栈       │ │     └──────────────────────┘     │                      │
│ │   - NvM存储      │ │                                  │                      │
│ └──────────────────┘ │                                  │                      │
└──────────────────────┘                                  └──────────────────────┘
```

### 2.2 嵌入式层模块划分

```
embedded/
├── src/
│   ├── ccc/                    ← CCC Digital Key 协议
│   │   ├── core/               ← 核心状态机 (CccDkCore, CccDkState)
│   │   ├── protocol/           ← 协议层 (CCC Protocol, UWB Ranging)
│   │   ├── ble/                ← BLE 通信 (KW47 BLE Stack)
│   │   ├── uwb/                ← UWB 驱动 (NCJ29D6/DW3000)
│   │   ├── nfc/                ← NFC 备选通信 (ST25R501)
│   │   ├── security/           ← 安全协议 (SecureChannel, Auth)
│   │   ├── keymgmt/            ← 密钥管理 (Key Derivation, Storage)
│   │   ├── transport/          ← 传输层 (PDU 封装/解封)
│   │   ├── services/           ← 服务层 (定位服务、时间同步)
│   │   └── logger/             ← 日志模块
│   ├── icce/                   ← ICCE 数字钥匙协议（中国标准）
│   ├── iccoa/                  ← ICOA 数字钥匙协议（行业组织）
│   ├── unified/                ← 统一抽象层 (多协议适配)
│   ├── hal/                    ← 硬件抽象层
│   │   ├── ble/                ← BLE HAL
│   │   ├── uwb/                ← UWB HAL (DW3000 SPI)
│   │   ├── nfc/                ← NFC HAL
│   │   ├── se/                 ← 安全芯片 HAL (SE050 I2C)
│   ├── crypto/                 ← 加密抽象 (ECC/AES/HKDF)
│   ├── security/               ← 安全引擎 + SCP03
│   ├── storage/                ← 密钥存储 (NvM + Flash)
│   └── common/                 ← 通用工具
│       ├── platform/           ← 平台相关
│       └── utils/              ← 工具函数
├── sdk/
│   ├── mbedtls/                ← mbedTLS 加密库
│   └── sm/                     ← 国密 SM2/SM3/SM4 实现
├── include/                    ← 公共头文件
├── tests/                      ← 单元测试
├── docs/                       ← 架构/设计文档
└── examples/                   ← 使用示例
```

---

## 3. CCC R2.0 核心协议栈

### 3.1 协议层次

```
┌──────────────────────────────────────────────────┐
│  Application Layer                               │
│  ├── Vehicle Access (解锁/启动/后备箱)             │
│  ├── Key Management (分发/撤销/更新)               │
│  └── Owner Sharing (钥匙共享/权限管理)              │
├──────────────────────────────────────────────────┤
│  Secure Channel Layer                            │
│  ├── ECDH P-256 密钥协商                          │
│  ├── ECDSA P-256 身份认证                          │
│  ├── AES-128-GCM 安全通信                          │
│  └── HKDF-SHA256 密钥派生                          │
├──────────────────────────────────────────────────┤
│  Transport Layer                                 │
│  ├── BLE Transport (Advertising/Scan/Connect)     │
│  ├── UWB Transport (Ranging/Data Transfer)       │
│  └── NFC Transport (Passive Entry)               │
├──────────────────────────────────────────────────┤
│  Link Layer                                      │
│  ├── KW47 BLE 5.4 Controller                     │
│  ├── NCJ29D6/DW3000 UWB Transceiver              │
│  └── ST25R501 NFC Reader                         │
└──────────────────────────────────────────────────┘
```

### 3.2 关键交互流程

```
手机                        KW47 车端
 │                          │
 │  BLE Advertising         │
 │<─────────────────────────│
 │                          │
 │  BLE Connect             │
 │─────────────────────────>│
 │                          │
 │  ECDH Key Exchange       │
 │══════════════════════════│  ← 使用 mbedTLS ECC
 │                          │
 │  ECDSA 身份认证           │
 │══════════════════════════│  ← 使用 SE050 签名
 │                          │
 │  AES-GCM Secure Channel  │
 │══════════════════════════│  ← 使用 SE050 加解密
 │                          │
 │  UWB Ranging (测距)      │
 │<────────────────────────>│
 │                          │
 │  Vehicle Command         │
 │  (Unlock/Start/Run)      │
 │══════════════════════════│
 │                          │
 │  车辆状态回调             │
 │─────────────────────────>│  ← 调用 yuleASR 诊断栈
 │                          │
```

---

## 4. 与 yuleASR 的集成点

| yuleASR 模块 | yuleDKCS 使用场景 | 状态 |
|:---|:---|:---:|
| **OS (调度)** | 任务调度、定时器管理、中断处理 | ✅ 已有 |
| **CSM/CryIf** | 加密操作接口 (AES/ECC/HKDF) | ✅ 已有 |
| **KeyM** | 密钥槽管理、安全存储 | ✅ 已有 |
| **NvM** | 数字钥匙持久化存储 | ✅ 已有 |
| **DCM/Dem** | 钥匙系统诊断、错误事件记录 | ✅ 已有 |
| **Det** | 开发错误跟踪、参数校验 | ✅ 已有 |
| **RTE** | SWC间通信、事件触发 | ✅ 已有 |
| **ASW** | 车辆控制应用逻辑 | ✅ 已有 |

---

## 5. 实施路线

### Phase 1 — 项目重构 (2周)
将现有 yuleDKCS 按 example 结构重新组织，建立清晰的依赖关系

### Phase 2 — 嵌入式核心 (3周)
- CCC R2.0 协议栈完善
- KW47 BLE/UWB 驱动整合
- SE050 SCP03 安全通道

### Phase 3 — 三端联调 (2周)
- 后端钥匙管理 API
- 移动端 SDK 对接
- 端到端场景验证

### Phase 4 — 文档与发布 (1周)
- 集成指南
- 硬件搭建教程
- 示例场景演示

---

## 6. 交付物清单

| 交付物 | 说明 | 阶段 |
|:---|:---|:---:|
| 项目结构 | 重构后的 yuleDKCS 目录 | P1 |
| CCC协议栈 | 完整 CCC R2.0 核心实现 | P2 |
| KW47驱动 | BLE/UWB/NFC 硬件驱动 | P2 |
| SE050集成 | 安全芯片 + SCP03 通道 | P2 |
| 后端API | 钥匙管理 REST API | P3 |
| 移动端App | iOS/Android/Flutter 参考实现 | P3 |
| 集成文档 | 硬件搭建、编译、部署指南 | P4 |
| 示例场景 | 演示/测试用 KeyFob 案例 | P4 |
